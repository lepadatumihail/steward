---
id: T12
title: Deploy pipeline and repo-publication timing
type: task
status: open
assignee:
blocked-by: [T05]
---

## Question

Stand up the live URL and settle publication mechanics:

- Vercel project + env wiring (server-side keys only), production URL that judges can open cold.
- Create the GitHub repo and decide **when it flips public** — public is required at submission; earlier flip = stronger timestamp evidence, later flip = field privacy. **Both repo creation and the public flip need the human's explicit OK** (outward-facing actions).
- Add the open-source license (MIT unless the human objects) — a Stage-One requirement.

Resolution records the live URL, repo URL, and the publication decision.
